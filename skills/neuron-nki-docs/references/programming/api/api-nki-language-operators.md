# nki.language — Operators (op specifiers)

> **Module**: nki.language
> **Functions**: 61

## Overview

Operator specifiers passed as the `op` argument to `nisa.activation()`, `nisa.tensor_tensor()`, `nisa.tensor_scalar()`, and `nisa.tensor_reduce()`.

## Functions

### nki.language.abs {#nki-language-abs}

`nki.language.abs()`

**Signature:**
```python
language.abs
```

Op specifier for `abs` operation. Pass as `op` argument to `nisa.activation()`, `nisa.tensor_tensor()`, `nisa.tensor_scalar()`, or `nisa.tensor_reduce()`.

---

### nki.language.abs_max {#nki-language-abs_max}

`nki.language.abs_max()`

**Signature:**
```python
language.abs_max
```

Op specifier for `abs_max` operation. Pass as `op` argument to `nisa.activation()`, `nisa.tensor_tensor()`, `nisa.tensor_scalar()`, or `nisa.tensor_reduce()`.

---

### nki.language.abs_min {#nki-language-abs_min}

`nki.language.abs_min()`

**Signature:**
```python
language.abs_min
```

Op specifier for `abs_min` operation. Pass as `op` argument to `nisa.activation()`, `nisa.tensor_tensor()`, `nisa.tensor_scalar()`, or `nisa.tensor_reduce()`.

---

### nki.language.add {#nki-language-add}

`nki.language.add()`

**Signature:**
```python
language.add
```

Op specifier for `add` operation. Pass as `op` argument to `nisa.activation()`, `nisa.tensor_tensor()`, `nisa.tensor_scalar()`, or `nisa.tensor_reduce()`.

---

### nki.language.arctan {#nki-language-arctan}

`nki.language.arctan()`

**Signature:**
```python
language.arctan
```

Op specifier for `arctan` operation. Pass as `op` argument to `nisa.activation()`, `nisa.tensor_tensor()`, `nisa.tensor_scalar()`, or `nisa.tensor_reduce()`.

---

### nki.language.average {#nki-language-average}

`nki.language.average()`

**Signature:**
```python
language.average
```

Op specifier for `average` operation. Pass as `op` argument to `nisa.activation()`, `nisa.tensor_tensor()`, `nisa.tensor_scalar()`, or `nisa.tensor_reduce()`.

---

### nki.language.bitwise_and {#nki-language-bitwise_and}

`nki.language.bitwise_and()`

**Signature:**
```python
language.bitwise_and
```

Op specifier for `bitwise_and` operation. Pass as `op` argument to `nisa.activation()`, `nisa.tensor_tensor()`, `nisa.tensor_scalar()`, or `nisa.tensor_reduce()`.

---

### nki.language.bitwise_or {#nki-language-bitwise_or}

`nki.language.bitwise_or()`

**Signature:**
```python
language.bitwise_or
```

Op specifier for `bitwise_or` operation. Pass as `op` argument to `nisa.activation()`, `nisa.tensor_tensor()`, `nisa.tensor_scalar()`, or `nisa.tensor_reduce()`.

---

### nki.language.bitwise_xor {#nki-language-bitwise_xor}

`nki.language.bitwise_xor()`

**Signature:**
```python
language.bitwise_xor
```

Op specifier for `bitwise_xor` operation. Pass as `op` argument to `nisa.activation()`, `nisa.tensor_tensor()`, `nisa.tensor_scalar()`, or `nisa.tensor_reduce()`.

---

### nki.language.bypass {#nki-language-bypass}

`nki.language.bypass()`

**Signature:**
```python
language.bypass
```

Op specifier for `bypass` operation. Pass as `op` argument to `nisa.activation()`, `nisa.tensor_tensor()`, `nisa.tensor_scalar()`, or `nisa.tensor_reduce()`.

---

### nki.language.ceil {#nki-language-ceil}

`nki.language.ceil()`

**Signature:**
```python
language.ceil
```

Op specifier for `ceil` operation. Pass as `op` argument to `nisa.activation()`, `nisa.tensor_tensor()`, `nisa.tensor_scalar()`, or `nisa.tensor_reduce()`.

---

### nki.language.copy {#nki-language-copy}

`nki.language.copy()`

**Signature:**
```python
language.copy
```

Op specifier for `copy` operation. Pass as `op` argument to `nisa.activation()`, `nisa.tensor_tensor()`, `nisa.tensor_scalar()`, or `nisa.tensor_reduce()`.

---

### nki.language.cos {#nki-language-cos}

`nki.language.cos()`

**Signature:**
```python
language.cos
```

Op specifier for `cos` operation. Pass as `op` argument to `nisa.activation()`, `nisa.tensor_tensor()`, `nisa.tensor_scalar()`, or `nisa.tensor_reduce()`.

---

### nki.language.divide {#nki-language-divide}

`nki.language.divide()`

**Signature:**
```python
language.divide
```

Op specifier for `divide` operation. Pass as `op` argument to `nisa.activation()`, `nisa.tensor_tensor()`, `nisa.tensor_scalar()`, or `nisa.tensor_reduce()`.

---

### nki.language.equal {#nki-language-equal}

`nki.language.equal()`

**Signature:**
```python
language.equal
```

Op specifier for `equal` operation. Pass as `op` argument to `nisa.activation()`, `nisa.tensor_tensor()`, `nisa.tensor_scalar()`, or `nisa.tensor_reduce()`.

---

### nki.language.erf {#nki-language-erf}

`nki.language.erf()`

**Signature:**
```python
language.erf
```

Op specifier for `erf` operation. Pass as `op` argument to `nisa.activation()`, `nisa.tensor_tensor()`, `nisa.tensor_scalar()`, or `nisa.tensor_reduce()`.

---

### nki.language.erf_dx {#nki-language-erf_dx}

`nki.language.erf_dx()`

**Signature:**
```python
language.erf_dx
```

Op specifier for `erf_dx` operation. Pass as `op` argument to `nisa.activation()`, `nisa.tensor_tensor()`, `nisa.tensor_scalar()`, or `nisa.tensor_reduce()`.

---

### nki.language.exp {#nki-language-exp}

`nki.language.exp()`

**Signature:**
```python
language.exp
```

Op specifier for `exp` operation. Pass as `op` argument to `nisa.activation()`, `nisa.tensor_tensor()`, `nisa.tensor_scalar()`, or `nisa.tensor_reduce()`.

---

### nki.language.floor {#nki-language-floor}

`nki.language.floor()`

**Signature:**
```python
language.floor
```

Op specifier for `floor` operation. Pass as `op` argument to `nisa.activation()`, `nisa.tensor_tensor()`, `nisa.tensor_scalar()`, or `nisa.tensor_reduce()`.

---

### nki.language.fmod {#nki-language-fmod}

`nki.language.fmod()`

**Signature:**
```python
language.fmod
```

Op specifier for `fmod` operation. Pass as `op` argument to `nisa.activation()`, `nisa.tensor_tensor()`, `nisa.tensor_scalar()`, or `nisa.tensor_reduce()`.

---

### nki.language.gelu {#nki-language-gelu}

`nki.language.gelu()`

**Signature:**
```python
language.gelu
```

Op specifier for `gelu` operation. Pass as `op` argument to `nisa.activation()`, `nisa.tensor_tensor()`, `nisa.tensor_scalar()`, or `nisa.tensor_reduce()`.

---

### nki.language.gelu_apprx_sigmoid {#nki-language-gelu_apprx_sigmoid}

`nki.language.gelu_apprx_sigmoid()`

**Signature:**
```python
language.gelu_apprx_sigmoid
```

Op specifier for `gelu_apprx_sigmoid` operation. Pass as `op` argument to `nisa.activation()`, `nisa.tensor_tensor()`, `nisa.tensor_scalar()`, or `nisa.tensor_reduce()`.

---

### nki.language.gelu_apprx_sigmoid_dx {#nki-language-gelu_apprx_sigmoid_dx}

`nki.language.gelu_apprx_sigmoid_dx()`

**Signature:**
```python
language.gelu_apprx_sigmoid_dx
```

Op specifier for `gelu_apprx_sigmoid_dx` operation. Pass as `op` argument to `nisa.activation()`, `nisa.tensor_tensor()`, `nisa.tensor_scalar()`, or `nisa.tensor_reduce()`.

---

### nki.language.gelu_apprx_tanh {#nki-language-gelu_apprx_tanh}

`nki.language.gelu_apprx_tanh()`

**Signature:**
```python
language.gelu_apprx_tanh
```

Op specifier for `gelu_apprx_tanh` operation. Pass as `op` argument to `nisa.activation()`, `nisa.tensor_tensor()`, `nisa.tensor_scalar()`, or `nisa.tensor_reduce()`.

---

### nki.language.gelu_dx {#nki-language-gelu_dx}

`nki.language.gelu_dx()`

**Signature:**
```python
language.gelu_dx
```

Op specifier for `gelu_dx` operation. Pass as `op` argument to `nisa.activation()`, `nisa.tensor_tensor()`, `nisa.tensor_scalar()`, or `nisa.tensor_reduce()`.

---

### nki.language.greater {#nki-language-greater}

`nki.language.greater()`

**Signature:**
```python
language.greater
```

Op specifier for `greater` operation. Pass as `op` argument to `nisa.activation()`, `nisa.tensor_tensor()`, `nisa.tensor_scalar()`, or `nisa.tensor_reduce()`.

---

### nki.language.greater_equal {#nki-language-greater_equal}

`nki.language.greater_equal()`

**Signature:**
```python
language.greater_equal
```

Op specifier for `greater_equal` operation. Pass as `op` argument to `nisa.activation()`, `nisa.tensor_tensor()`, `nisa.tensor_scalar()`, or `nisa.tensor_reduce()`.

---

### nki.language.invert {#nki-language-invert}

`nki.language.invert()`

**Signature:**
```python
language.invert
```

Op specifier for `invert` operation. Pass as `op` argument to `nisa.activation()`, `nisa.tensor_tensor()`, `nisa.tensor_scalar()`, or `nisa.tensor_reduce()`.

---

### nki.language.left_shift {#nki-language-left_shift}

`nki.language.left_shift()`

**Signature:**
```python
language.left_shift
```

Op specifier for `left_shift` operation. Pass as `op` argument to `nisa.activation()`, `nisa.tensor_tensor()`, `nisa.tensor_scalar()`, or `nisa.tensor_reduce()`.

---

### nki.language.less {#nki-language-less}

`nki.language.less()`

**Signature:**
```python
language.less
```

Op specifier for `less` operation. Pass as `op` argument to `nisa.activation()`, `nisa.tensor_tensor()`, `nisa.tensor_scalar()`, or `nisa.tensor_reduce()`.

---

### nki.language.less_equal {#nki-language-less_equal}

`nki.language.less_equal()`

**Signature:**
```python
language.less_equal
```

Op specifier for `less_equal` operation. Pass as `op` argument to `nisa.activation()`, `nisa.tensor_tensor()`, `nisa.tensor_scalar()`, or `nisa.tensor_reduce()`.

---

### nki.language.log {#nki-language-log}

`nki.language.log()`

**Signature:**
```python
language.log
```

Op specifier for `log` operation. Pass as `op` argument to `nisa.activation()`, `nisa.tensor_tensor()`, `nisa.tensor_scalar()`, or `nisa.tensor_reduce()`.

---

### nki.language.logical_and {#nki-language-logical_and}

`nki.language.logical_and()`

**Signature:**
```python
language.logical_and
```

Op specifier for `logical_and` operation. Pass as `op` argument to `nisa.activation()`, `nisa.tensor_tensor()`, `nisa.tensor_scalar()`, or `nisa.tensor_reduce()`.

---

### nki.language.logical_not {#nki-language-logical_not}

`nki.language.logical_not()`

**Signature:**
```python
language.logical_not
```

Op specifier for `logical_not` operation. Pass as `op` argument to `nisa.activation()`, `nisa.tensor_tensor()`, `nisa.tensor_scalar()`, or `nisa.tensor_reduce()`.

---

### nki.language.logical_or {#nki-language-logical_or}

`nki.language.logical_or()`

**Signature:**
```python
language.logical_or
```

Op specifier for `logical_or` operation. Pass as `op` argument to `nisa.activation()`, `nisa.tensor_tensor()`, `nisa.tensor_scalar()`, or `nisa.tensor_reduce()`.

---

### nki.language.logical_xor {#nki-language-logical_xor}

`nki.language.logical_xor()`

**Signature:**
```python
language.logical_xor
```

Op specifier for `logical_xor` operation. Pass as `op` argument to `nisa.activation()`, `nisa.tensor_tensor()`, `nisa.tensor_scalar()`, or `nisa.tensor_reduce()`.

---

### nki.language.maximum {#nki-language-maximum}

`nki.language.maximum()`

**Signature:**
```python
language.maximum
```

Op specifier for `maximum` operation. Pass as `op` argument to `nisa.activation()`, `nisa.tensor_tensor()`, `nisa.tensor_scalar()`, or `nisa.tensor_reduce()`.

---

### nki.language.minimum {#nki-language-minimum}

`nki.language.minimum()`

**Signature:**
```python
language.minimum
```

Op specifier for `minimum` operation. Pass as `op` argument to `nisa.activation()`, `nisa.tensor_tensor()`, `nisa.tensor_scalar()`, or `nisa.tensor_reduce()`.

---

### nki.language.mish {#nki-language-mish}

`nki.language.mish()`

**Signature:**
```python
language.mish
```

Op specifier for `mish` operation. Pass as `op` argument to `nisa.activation()`, `nisa.tensor_tensor()`, `nisa.tensor_scalar()`, or `nisa.tensor_reduce()`.

---

### nki.language.mod {#nki-language-mod}

`nki.language.mod()`

**Signature:**
```python
language.mod
```

Op specifier for `mod` operation. Pass as `op` argument to `nisa.activation()`, `nisa.tensor_tensor()`, `nisa.tensor_scalar()`, or `nisa.tensor_reduce()`.

---

### nki.language.multiply {#nki-language-multiply}

`nki.language.multiply()`

**Signature:**
```python
language.multiply
```

Op specifier for `multiply` operation. Pass as `op` argument to `nisa.activation()`, `nisa.tensor_tensor()`, `nisa.tensor_scalar()`, or `nisa.tensor_reduce()`.

---

### nki.language.negative {#nki-language-negative}

`nki.language.negative()`

**Signature:**
```python
language.negative
```

Op specifier for `negative` operation. Pass as `op` argument to `nisa.activation()`, `nisa.tensor_tensor()`, `nisa.tensor_scalar()`, or `nisa.tensor_reduce()`.

---

### nki.language.not_equal {#nki-language-not_equal}

`nki.language.not_equal()`

**Signature:**
```python
language.not_equal
```

Op specifier for `not_equal` operation. Pass as `op` argument to `nisa.activation()`, `nisa.tensor_tensor()`, `nisa.tensor_scalar()`, or `nisa.tensor_reduce()`.

---

### nki.language.power {#nki-language-power}

`nki.language.power()`

**Signature:**
```python
language.power
```

Op specifier for `power` operation. Pass as `op` argument to `nisa.activation()`, `nisa.tensor_tensor()`, `nisa.tensor_scalar()`, or `nisa.tensor_reduce()`.

---

### nki.language.prelu {#nki-language-prelu}

`nki.language.prelu()`

**Signature:**
```python
language.prelu
```

Op specifier for `prelu` operation. Pass as `op` argument to `nisa.activation()`, `nisa.tensor_tensor()`, `nisa.tensor_scalar()`, or `nisa.tensor_reduce()`.

---

### nki.language.reciprocal {#nki-language-reciprocal}

`nki.language.reciprocal()`

**Signature:**
```python
language.reciprocal
```

Op specifier for `reciprocal` operation. Pass as `op` argument to `nisa.activation()`, `nisa.tensor_tensor()`, `nisa.tensor_scalar()`, or `nisa.tensor_reduce()`.

---

### nki.language.relu {#nki-language-relu}

`nki.language.relu()`

**Signature:**
```python
language.relu
```

Op specifier for `relu` operation. Pass as `op` argument to `nisa.activation()`, `nisa.tensor_tensor()`, `nisa.tensor_scalar()`, or `nisa.tensor_reduce()`.

---

### nki.language.right_shift {#nki-language-right_shift}

`nki.language.right_shift()`

**Signature:**
```python
language.right_shift
```

Op specifier for `right_shift` operation. Pass as `op` argument to `nisa.activation()`, `nisa.tensor_tensor()`, `nisa.tensor_scalar()`, or `nisa.tensor_reduce()`.

---

### nki.language.rsqrt {#nki-language-rsqrt}

`nki.language.rsqrt()`

**Signature:**
```python
language.rsqrt
```

Op specifier for `rsqrt` operation. Pass as `op` argument to `nisa.activation()`, `nisa.tensor_tensor()`, `nisa.tensor_scalar()`, or `nisa.tensor_reduce()`.

---

### nki.language.sigmoid {#nki-language-sigmoid}

`nki.language.sigmoid()`

**Signature:**
```python
language.sigmoid
```

Op specifier for `sigmoid` operation. Pass as `op` argument to `nisa.activation()`, `nisa.tensor_tensor()`, `nisa.tensor_scalar()`, or `nisa.tensor_reduce()`.

---

### nki.language.sign {#nki-language-sign}

`nki.language.sign()`

**Signature:**
```python
language.sign
```

Op specifier for `sign` operation. Pass as `op` argument to `nisa.activation()`, `nisa.tensor_tensor()`, `nisa.tensor_scalar()`, or `nisa.tensor_reduce()`.

---

### nki.language.silu {#nki-language-silu}

`nki.language.silu()`

**Signature:**
```python
language.silu
```

Op specifier for `silu` operation. Pass as `op` argument to `nisa.activation()`, `nisa.tensor_tensor()`, `nisa.tensor_scalar()`, or `nisa.tensor_reduce()`.

---

### nki.language.silu_dx {#nki-language-silu_dx}

`nki.language.silu_dx()`

**Signature:**
```python
language.silu_dx
```

Op specifier for `silu_dx` operation. Pass as `op` argument to `nisa.activation()`, `nisa.tensor_tensor()`, `nisa.tensor_scalar()`, or `nisa.tensor_reduce()`.

---

### nki.language.sin {#nki-language-sin}

`nki.language.sin()`

**Signature:**
```python
language.sin
```

Op specifier for `sin` operation. Pass as `op` argument to `nisa.activation()`, `nisa.tensor_tensor()`, `nisa.tensor_scalar()`, or `nisa.tensor_reduce()`.

---

### nki.language.softplus {#nki-language-softplus}

`nki.language.softplus()`

**Signature:**
```python
language.softplus
```

Op specifier for `softplus` operation. Pass as `op` argument to `nisa.activation()`, `nisa.tensor_tensor()`, `nisa.tensor_scalar()`, or `nisa.tensor_reduce()`.

---

### nki.language.sqrt {#nki-language-sqrt}

`nki.language.sqrt()`

**Signature:**
```python
language.sqrt
```

Op specifier for `sqrt` operation. Pass as `op` argument to `nisa.activation()`, `nisa.tensor_tensor()`, `nisa.tensor_scalar()`, or `nisa.tensor_reduce()`.

---

### nki.language.square {#nki-language-square}

`nki.language.square()`

**Signature:**
```python
language.square
```

Op specifier for `square` operation. Pass as `op` argument to `nisa.activation()`, `nisa.tensor_tensor()`, `nisa.tensor_scalar()`, or `nisa.tensor_reduce()`.

---

### nki.language.subtract {#nki-language-subtract}

`nki.language.subtract()`

**Signature:**
```python
language.subtract
```

Op specifier for `subtract` operation. Pass as `op` argument to `nisa.activation()`, `nisa.tensor_tensor()`, `nisa.tensor_scalar()`, or `nisa.tensor_reduce()`.

---

### nki.language.tan {#nki-language-tan}

`nki.language.tan()`

**Signature:**
```python
language.tan
```

Op specifier for `tan` operation. Pass as `op` argument to `nisa.activation()`, `nisa.tensor_tensor()`, `nisa.tensor_scalar()`, or `nisa.tensor_reduce()`.

---

### nki.language.tanh {#nki-language-tanh}

`nki.language.tanh()`

**Signature:**
```python
language.tanh
```

Op specifier for `tanh` operation. Pass as `op` argument to `nisa.activation()`, `nisa.tensor_tensor()`, `nisa.tensor_scalar()`, or `nisa.tensor_reduce()`.

---

### nki.language.trunc {#nki-language-trunc}

`nki.language.trunc()`

**Signature:**
```python
language.trunc
```

Op specifier for `trunc` operation. Pass as `op` argument to `nisa.activation()`, `nisa.tensor_tensor()`, `nisa.tensor_scalar()`, or `nisa.tensor_reduce()`.

---
