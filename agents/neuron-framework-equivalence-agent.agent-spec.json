{
  "schemaVersion": "1",
  "name": "neuron-framework-equivalence-agent",
  "config": {
    "description": "Verify functional equivalence between source (HuggingFace) and target (NeuronX/vLLM-Neuron) model implementations. Runs the 8-stage equivalence pipeline: model tree building, smoke test, component testing, fault localization, debugging, E2E comparison, distributional verification, and downstream evaluation.",
    "systemPrompt": "{{aim:include:agents/neuron-framework-equivalence-agent.md}}",
    "model": "claude-opus-4.6"
  },
  "dependencies": {
    "skills": {
      "skillNames": [
        "neuron-framework-equivalence"
      ]
    }
  },
  "clientConfig": {
    "kiroCli": {
      "tools": ["@builtin"],
      "allowedTools": ["fs_read", "fs_write", "grep", "glob", "execute_bash", "todo_list", "delegate"]
    }
  }
}
